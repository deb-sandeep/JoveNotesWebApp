expect( array of questions )

[not] to have a particular question
.toHaveQuestion( id )

to have question a before b
.toHaveQuestionsInOrder( [a, b] ) ;

to have question in descending order of attribute value
.toHaveDescendingAttributeValue( attribute name )

to have question in ascending order of attribute value
.toHaveAscendingAttributeValue( attribute name )
